{
  "name": "WALTER",
  "displayName": "WALTER",
  "description": "Provides full feature support for WALTER language",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.134.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "main": "./out/extension.js",
  "activationEvents": [],
  "scripts": {
    "lint": "eslint src",
    "watch": "tsc -watch -p ./",
    "compile": "tsc -p ./",
    "validate-syntax-grammar": "textmate-validate --verbose ./configs/syntax-patterns.json",
    "test-syntax-grammar": "vscode-tmgrammar-test testcases ./tests/**/*.test.txt -g ./configs/syntax-patterns.json",
    "pretest": "npm run compile && npm run lint",
    "test": "npm run validate-syntax-grammar && npm run test-syntax-grammar && vscode-test && tree-sitter test",
    "parser:build": "npm --prefix parser run generate && npm --prefix parser run build",
    "vscode:prepublish": "npm run parser:build && npm run compile"
  },
  "contributes": {
    "commands": [
      {
        "command": "walter.helloWorld",
        "title": "Hello World"
      }
    ],
    "languages": [
      {
        "id": "walter",
        "filenames": [
          "rtconfig.txt"
        ],
        "icon": {
          "light": "./assets/walter-icon.png",
          "dark": "./assets/walter-icon.png"
        },
        "configuration": "./configs/language-configuration.json"
      }
    ],
    "grammars": [
      {
        "language": "walter",
        "scopeName": "source.walter",
        "path": "./configs/syntax-patterns.json"
      }
    ]
  },
  "dependencies": {
    "web-tree-sitter": "^0.26.13"
  },
  "devDependencies": {
    "@carlwr/textmate-validate": "^1.0.1",
    "@types/emscripten": "^1.41.5",
    "@types/mocha": "^10.0.10",
    "@types/node": "24.x",
    "@types/vscode": "^1.134.0",
    "@vscode/test-cli": "^0.0.15",
    "@vscode/test-electron": "^3.0.0",
    "eslint": "^10.5.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.1",
    "vscode-tmgrammar-test": "^0.1.3"
  },
  "overrides": {
    "diff": "^8.0.4",
    "serialize-javascript": "^7.0.6"
  }
}
